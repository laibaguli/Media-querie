Files
index.html: Contains the HTML structure of the webpage.
style.css: Defines the styles and media queries.

Features
Displays a heading (h1) and a paragraph (p) with basic styling.
Uses CSS media queries to change the background color and text color based on screen width.
